{
	"name": "@noname/general-soul-samsara",
	"version": "0.1.0",
	"private": true,
	"description": "将魂轮回：基于 noname 引擎的单人卡牌构筑肉鸽（M0 原型）",
	"type": "module",
	"license": "GPL-3.0-only",
	"scripts": {
		"build": "vite build",
		"build:dev": "vite build --mode development",
		"build:watch": "vite build --watch --mode development",
		"lint": "pnpm eslint src/** tests/**",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:e2e": "pnpm build:dev && playwright test"
	},
	"dependencies": {
		"noname": "workspace:*"
	},
	"devDependencies": {
		"@playwright/test": "^1.50.0",
		"vite": "^7.3.1",
		"vitest": "^3.2.0"
	}
}
